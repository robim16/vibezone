services:
- type: web
  name: next-js
  runtime: node
  plan: starter
  buildCommand: npm; npm run build
  startCommand: npm start
  autoDeploy: false
  envVars:
  - key: NODE_ENV
    value: production